name: Release Sandbox Mode

on:
  push:
    branches: [main]
    paths:
      - "SandboxMode/**"
      - ".github/workflows/release-sandbox.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create or update release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="sandbox-v0.3.74"
          asset="SandboxMode/release/SandboxMode.dll"
          if gh release view "$tag" >/dev/null 2>&1; then
            gh release upload "$tag" "$asset" --clobber
          else
            gh release create "$tag" "$asset" --latest=false --title "Disfigure Sandbox Mode 0.3.74" --notes "Adds timer controls up to 24 hours and reversible stacks for Robust, Refine, Pact, and Forge. Press F5 during a run to open Sandbox Mode."
          fi

